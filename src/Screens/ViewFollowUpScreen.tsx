import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ViewFollowUpScreen = () => {
  return (
    <View>
      <Text>ViewFollowUpScreen</Text>
    </View>
  );
};

export default ViewFollowUpScreen;

const styles = StyleSheet.create({});
