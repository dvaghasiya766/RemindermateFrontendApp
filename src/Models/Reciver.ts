export default class Reciver {
  name: string;
  email: string;
  color: string;

  constructor(name: string, email: string, color: string) {
    this.name = name;
    this.email = email;
    this.color = color;
  }
}
